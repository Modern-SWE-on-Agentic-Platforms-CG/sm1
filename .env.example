# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smarthiremain001
DB_USER=postgres
DB_PASSWORD=

# JWT
JWT_SECRET=change-me-to-a-strong-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# Frontend
VITE_API_BASE_URL=http://localhost:8000

# CORS
CORS_ORIGINS=http://localhost:5173

# File Storage
UPLOAD_DIR=backend/uploads

# Email / SMTP (set SMTP_ENABLED=true to send real emails)
SMTP_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_FROM=noreply@smartrecruit.local
